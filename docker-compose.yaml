version: "3"

services:
  db:
    image: postgres:14.13-alpine3.20
    restart: always
    ports:
      - "5432:5432"
    env_file:
      - .env
    # volumes:
    #   - db_data:/var/lib/postgresql/data
